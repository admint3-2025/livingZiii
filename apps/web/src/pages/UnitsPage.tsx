import React from 'react';
import { ComingSoonPage } from './ComingSoonPage';

export const UnitsPage: React.FC = () => {
  return (
    <ComingSoonPage
      title="Unidades"
      description="Alta y administración de unidades (número, ocupación, residentes) para habilitar estados de cuenta y cobranza."
    />
  );
};

